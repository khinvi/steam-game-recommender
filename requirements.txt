# requirements.txt

# Core
fastapi==0.104.0
uvicorn[standard]==0.23.0
python-dotenv==1.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0

# Database (Free)
sqlalchemy==2.0.0
# sqlite3 is built-in to Python
# psycopg2-binary==2.9.0  # Optional: For PostgreSQL

# Caching (Free)
redis==5.0.0  # Optional: For Redis
cachetools==5.3.0  # In-memory cache fallback

# ML/Data Science (Free)
numpy==1.24.0
pandas==2.0.0
scikit-learn==1.3.0
scipy==1.11.0
torch==2.0.0  # CPU version is free
# For GPU (if you have NVIDIA GPU):
# torch==2.0.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html

# Vector Database (Free)
chromadb>=0.4.0

# Authentication (Free)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# API Rate Limiting (Free)
slowapi==0.1.8

# Testing (Free)
pytest==7.4.0
pytest-asyncio==0.21.0
httpx==0.24.0

# Monitoring (Free - Optional)
prometheus-client==0.17.0

# Utilities
tqdm==4.66.0
Pillow==10.0.0
matplotlib==3.7.0
seaborn==0.12.0

# Development (Free)
black==23.7.0
flake8==6.1.0
mypy==1.5.0
pre-commit==3.3.0